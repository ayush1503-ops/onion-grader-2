# OnionGrader - Vercel/serverless runtime dependencies
# PINNED versions chosen to stay under Vercel's 250 MB function limit
# (default newest wheels measure ~259 MB -> deploy fails; these ~237 MB)
# Training tools: requirements-ml.txt | PDF API: requirements-api.txt
flask>=3.0.0
opencv-python-headless==4.11.0.86
numpy==2.2.6
pillow==11.2.1
waitress>=3.0.0
# OPTIONAL but recommended: lets the SERVER read iPhone HEIC/HEIF photos
# directly (the browser already converts them to JPEG before upload, so
# this is only a safety net for API clients that post raw HEIC).
pillow-heif>=0.16.0
